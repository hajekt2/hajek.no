---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Tomas Hajek",
      url: "https://hajek.no/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Tomas Hajek",
      logo: {
        "@type": "ImageObject",
        url: "https://hajek.no/tomas-hajek-profile.png",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://hajek.no/tomas-hajek-profile.png",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Tomas Hajek",
    url: "https://hajek.no",
    image: "https://hajek.no/tomas-hajek-profile.png",
    sameAs: [
      "https://github.com/hajekt2",
      "https://x.com/hajekt21",
      "https://bsky.app/profile/hajek.no",
    ],
    jobTitle: "Software Engineer",
    description:
      "CCM architecture, document systems, AI tools, and pragmatic engineering from Norway.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Tomas Hajek",
    url: "https://hajek.no",
    description:
      "CCM architecture, document systems, AI tools, and pragmatic engineering from Norway.",
    author: {
      "@type": "Person",
      name: "Tomas Hajek",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://hajek.no/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />